<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- App Sandbox (disabled for full system access) -->
    <!-- Note: Menu bar apps with accessibility needs typically run unsandboxed -->
    <key>com.apple.security.app-sandbox</key>
    <false/>

    <!-- Hardened Runtime (required for notarization) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow DYLD environment variables (for Python framework) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>

    <!-- Disable library validation (for bundled Python packages) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Allow JIT (for numpy/scipy) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Microphone access -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Accessibility (for keyboard simulation, paste) -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>

    <!-- Network access (for API calls) -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- File access (for config, temp files) -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Keychain access (for API key storage) -->
    <key>com.apple.security.keychain-access-groups</key>
    <array>
        <string>$(AppIdentifierPrefix)com.whisperhud.app</string>
    </array>
</dict>
</plist>
